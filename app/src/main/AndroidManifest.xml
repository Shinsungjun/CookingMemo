<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kau.holyjoon.cookingmemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FolderActivity2"
            android:theme="@style/MyDialog">
        </activity>
        <activity android:name=".NewFolderActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name = "kau.holyjoon.cookingmemo.EditActivity"/>
        <activity android:name="kau.holyjoon.cookingmemo.PlusActivity" android:theme="@style/MyDialog"/>
        <activity android:name=".Choice_ingredient_Main"/>
        <activity android:name=".Db_AddActivity"/>
        <activity android:name=".ingredientMainActivity"/>
    </application>
</manifest>